# System packages we'll need to install in our Docker image.
# We'll use this list to purge (most of the) packages as well.
gcc
libc6-dev
libcurl4-openssl-dev
libssl-dev
make
python3
python3-dev
python3-pip
python3-setuptools
python3-wheel
python3-venv
